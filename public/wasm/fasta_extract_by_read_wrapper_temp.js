/**
 * Wrapper function for the fasta_extract_by_read WASM module.
 * Automatically generated by generate_wrapper.py
 */

(function() {
  /**
   * Runs the FastaExtractByRead tool with the provided input data.
   * @param {string} inputData - The input data.
   * @param {Array<string>} args - Additional arguments to pass to fasta_extract_by_read.
   * @returns {Promise<Object>} An object containing stdout and stderr outputs.
   */
  async function runFastaExtractByRead(inputData, args = []) {
    console.log("Starting runFastaExtractByRead with input:", inputData);
    console.log("Arguments:", args);

    try {
      // Normalize line endings
      inputData = inputData.replace(/\r\n/g, '\n');

      // Buffers for capturing stdout and stderr
      let stdoutBuffer = '';
      let stderrBuffer = '';

      // Initialize options for the module
      const options = {
        locateFile: (path) => {
          if (path.endsWith('.wasm')) {
            return `/wasm/${path}`;
          }
          return path;
        },
        thisProgram: './fasta_extract_by_read',
        noInitialRun: true, // We will call main manually
        // Remove noExitRuntime to use default behavior (false)
        // noExitRuntime: false,
        print: function(text) {
          stdoutBuffer += text + '\n';
        },
        printErr: function(text) {
          stderrBuffer += text + '\n';
        },
      };

      // Load the module
      await loadModuleScript('fasta_extract_by_read');
      const moduleFactory = window['fasta_extract_by_read'];
      if (typeof moduleFactory !== 'function') {
        throw new Error("Module factory function for fasta_extract_by_read is not available.");
      }

      const module = await moduleFactory(options);

      // Write input data to 'input.txt' in the virtual filesystem
      let fullArgs = args;

      module.FS.writeFile('input.txt', inputData);
      console.log("Executing module.callMain with arguments:", fullArgs);
      module.callMain(fullArgs);

      // Single-output: capture stdout normally.
      const outputData = stdoutBuffer.trim();
      return {
        stdout: outputData,
        stderr: stderrBuffer.trim()
      };

    } catch (err) {
      console.error('Error in runFastaExtractByRead:', err);
      throw err;
    }
  }

  /**
   * Dynamically loads the WASM module script if not already loaded.
   * @param {string} moduleName - The name of the module.
   */
  function loadModuleScript(moduleName) {
    return new Promise((resolve, reject) => {
      if (window[moduleName]) {
        // Module script is already loaded
        resolve();
        return;
      }

      const script = document.createElement('script');
      script.src = `/wasm/${moduleName}.js`;
      script.onload = () => {
        console.log(`Module script ${moduleName}.js loaded.`);
        resolve();
      };
      script.onerror = () => {
        reject(new Error(`Failed to load module script ${moduleName}.js.`));
      };
      document.head.appendChild(script);
    });
  }

  /**
   * Expose the runFastaExtractByRead function globally.
   */
  window.run_fasta_extract_by_read = runFastaExtractByRead;
})();